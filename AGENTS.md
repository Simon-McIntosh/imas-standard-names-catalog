# AGENTS.md — AI Agent Instructions

## Overview

This is a **data-only repository** containing the IMAS Standard Names catalog.

**Contents:**
- `standard_names/` — YAML catalog files (one per physics domain)
- `catalog.yml` — export metadata and provenance
- `.github/workflows/` — CI automation
- `src/` — Python package for distributing `catalog.db`

## Rules

### Editing

YAML files are generated by [imas-codex](https://github.com/Simon-McIntosh/imas-codex)
via `sn export`. Edit via PR — the `sn import` command detects changes and marks
them as `origin=catalog_edit` so the pipeline preserves human edits.

### What You Can Do

- Update documentation (README, AGENTS.md)
- Modify CI/CD workflows in `.github/workflows/`
- Update `.gitignore` for build artifacts

### What You Cannot Do

- Edit YAML content directly (use imas-codex `sn export` / `sn import`)
- Add Python dependencies beyond `imas-standard-names`
- Create application logic

## Dependency Pinning

The single `imas-standard-names` dependency in `pyproject.toml` MUST pin a
published stable release from PyPI:

```toml
[dependency-groups]
dev = [
    "imas-standard-names[quality,docs]==0.8.1",
]
```

**Release update rule:**

- This is a **data repository**. Do not churn the dependency for upstream
  release candidates; prerelease validation belongs upstream.
- Each new published stable release requires a deliberate version bump here so
  catalog builds use a reviewed, immutable tool release.
- `uv.lock` is **gitignored** (see `.gitignore`). The version constraint in
  `pyproject.toml` is therefore the repository's dependency record.
- The catalog-site CI (`catalog.yml`) derives `v<version>` from this pin and
  checks out `Simon-McIntosh/imas-standard-names` at that tag under `_isn/site`.
  The same pin therefore governs both the `standard-names` validation/build CLI
  and the bundled SPA source.
- Reproducibility for **tagged releases** is delivered by the tag on
  THIS repo plus the published `imas-standard-names` version recorded here.

**Do not** replace the stable version with a release candidate, branch, or
direct git reference, and **do not** add `uv.lock` to git. If a different
upstream version is required for a one-off investigation, keep that override
outside `pyproject.toml`.

## CI/CD

| Workflow | Trigger | Action |
|----------|---------|--------|
| `validate.yml` | PR, push to main | Validates YAML syntax and catalog consistency |
| `catalog.yml` | Push to main/tags | Builds versioned docs site via mike → gh-pages |
| `release.yml` | Tag `v*` | Builds `catalog.db`, `standard_names.zip`, Python wheel |

For same-repository review PRs, the PR body must contain its deployed `pr-<number>/` URL; that body link is the reviewer's pointer and CI fails if it is absent.
External-fork reviews cannot publish a page, so CI uploads the preview and comments with the artifact link instead.

## Git Workflow

```bash
git add standard_names/ catalog.yml
git commit -m "sn: update catalog entries"
git push origin main
```

**Remember**: This is a data repository. All generation tools live in `imas-codex`.
