[project]
name = "imas-standard-names-catalog"
dynamic = ["version"]
description = "IMAS Standard Names Catalog - pre-built SQLite database"
readme = "README.md"
authors = [
    { name = "Simon McIntosh", email = "simon.mcintosh@iter.org" }
]
license = { text = "CC BY-ND 4.0" }
requires-python = ">=3.13"
dependencies = []

[project.urls]
"Documentation" = "https://simon-mcintosh.github.io/imas-standard-names-catalog/"
"Repository" = "https://github.com/Simon-McIntosh/imas-standard-names-catalog"

[dependency-groups]
# Track imas-standard-names @main rather than a pinned RC tag. The
# catalog is a data-only repo and SHOULD NOT churn on every ISN RC.
# `uv.lock` is gitignored, so `uv run` always re-resolves and pulls
# whatever main currently points to. See AGENTS.md "Dependency Pinning".
dev = [
    "imas-standard-names[quality,docs] @ git+https://github.com/Simon-McIntosh/imas-standard-names.git@main",
]

[tool.hatch.version]
source = "vcs"

[tool.hatch.build.hooks.vcs]
version-file = "src/imas_standard_names_catalog/_version.py"

[tool.hatch.build.targets.wheel]
packages = ["src/imas_standard_names_catalog"]
artifacts = [
    "src/imas_standard_names_catalog/catalog.db",
]

[tool.hatch.build.targets.sdist]
include = [
    "src/",
    "standard_names/",
    "README.md",
    "pyproject.toml",
]

[build-system]
requires = ["hatchling", "hatch-vcs"]
build-backend = "hatchling.build"
